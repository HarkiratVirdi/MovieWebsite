<div class="contentsection mb-xl mt-xl">
    <div class="contentsection_banner">
        <div class="h-3-vw h-3--white m-abs">Watch the best films of 2021</div>
    </div>
    <div class="contentsection_images">
        <div class="contentsection_images_container">

        {{#each images}}
               <figure class="progressive">
  <img alt="contentImages" class="progressive__img progressive--not-loaded" data-progressive={{this.comp}} src="{{this.comp}}">
</figure>
        {{/each}}
        {{#each images}}
                      <figure class="progressive">
  <img alt="contentImages" class="progressive__img progressive--not-loaded" data-progressive={{this.comp}} src="{{this.comp}}">
</figure>
        {{/each}}
        {{#each images}}
                       <figure class="progressive">
  <img alt="contentImages" class="progressive__img progressive--not-loaded" data-progressive={{this.comp}} src="{{this.comp}}">
</figure>
        {{/each}}
        {{#each images}}
                       <figure class="progressive">
  <img alt="contentImages" class="progressive__img progressive--not-loaded" data-progressive={{this.comp}} src="{{this.comp}}">
</figure>
        {{/each}}

        </div>
    </div>
</div>